@using Epam.Task06.DependencyResolver
@{
    if (IsPost)
    {
        string name = Request["authName"];
        DependencyResolver.AuthUserLogic.ChangeAdmin(name);
        Response.Redirect("~");
    }
}

